---
phase: 02-networking
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - infra/access_iq_infra/settings.py
  - infra/config/dev.json
  - infra/config/prod.json
  - tests/unit/test_lake_stack.py
  - tests/unit/test_ecr_stack.py
  - tests/unit/test_secrets_stack.py
  - tests/unit/test_catalog_stack.py
  - tests/unit/test_app_synth.py
autonomous: true
requirements:
  - REQ-NET-01
  - REQ-NET-02

must_haves:
  truths:
    - "EnvConfig accepts a `vpc` dict field without breaking any existing stack or test"
    - "Config files contain VPC CIDRs per D1 (Trust 10.0.0.0/16, Platform 10.10.0.0/16)"
    - "Config files contain trust_account_id in iam section for peering role ARN construction"
    - "`make test` passes with all existing tests still green after EnvConfig change"
  artifacts:
    - path: "infra/access_iq_infra/settings.py"
      provides: "EnvConfig with vpc field"
      contains: "vpc: dict[str, Any]"
    - path: "infra/config/dev.json"
      provides: "Dev VPC config + trust_account_id"
      contains: "platform_cidr"
    - path: "infra/config/prod.json"
      provides: "Prod VPC config + trust_account_id"
      contains: "platform_cidr"
  key_links:
    - from: "infra/access_iq_infra/settings.py"
      to: "infra/config/dev.json"
      via: "load_env_config reads vpc section"
      pattern: "vpc=dict\\(raw\\.get"
---

<objective>
Extend EnvConfig with a `vpc` field and update both environment config files with VPC CIDRs (per D1) and `trust_account_id` (for peering role ARN). Update all 5 existing test `_cfg()` helpers to include `vpc={}` so the frozen dataclass change does not break existing tests.

Purpose: NetworkStack (Plan 02) requires `cfg.vpc` and `cfg.iam["trust_account_id"]` at construction time. This plan ensures the config foundation exists and all existing tests remain green before any networking code lands.

Output: Modified settings.py, both config JSONs, 5 updated test files.
</objective>

<execution_context>
@.claude/get-shit-done/workflows/execute-plan.md
@.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-networking/02-CONTEXT.md
@.planning/phases/02-networking/02-RESEARCH.md
@.planning/phases/02-networking/02-PATTERNS.md

<interfaces>
<!-- Current EnvConfig — executor modifies this exact dataclass -->
From infra/access_iq_infra/settings.py:
```python
@dataclass(frozen=True)
class EnvConfig:
    app_name: str
    env_name: str
    user_name: str
    account_id: str
    region: str
    s3: dict[str, Any]
    iam: dict[str, Any]
    tags: dict[str, str]
```

From infra/access_iq_infra/settings.py (load_env_config return):
```python
return EnvConfig(
    app_name=str(raw["app_name"]),
    env_name=str(raw["env_name"]),
    user_name=str(raw["user_name"]),
    account_id=str(raw["account_id"]),
    region=str(raw["region"]),
    s3=dict(raw.get("s3", {})),
    iam=dict(raw.get("iam", {})),
    tags=dict(raw.get("tags", {})),
)
```

From tests/unit/test_lake_stack.py (_cfg pattern used in all 5 test files):
```python
def _cfg(env_name: str = "dev") -> EnvConfig:
    return EnvConfig(
        app_name="access-iq",
        env_name=env_name,
        user_name="AWSReservedSSO_test/test",
        account_id="111111111111",
        region="eu-west-2",
        s3={},
        iam={"external_bucket": "northshire-trust-external-exports"},
        tags={"Environment": env_name, "Project": "access-iq"},
    )
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EnvConfig and config files with vpc + trust_account_id</name>
  <files>
    infra/access_iq_infra/settings.py,
    infra/config/dev.json,
    infra/config/prod.json
  </files>
  <read_first>
    infra/access_iq_infra/settings.py,
    infra/config/dev.json,
    infra/config/prod.json
  </read_first>
  <action>
1. In `infra/access_iq_infra/settings.py`, add `vpc: dict[str, Any]` field to the `EnvConfig` dataclass after `iam` and before `tags` (line 16 area). In `load_env_config`, add `vpc=dict(raw.get("vpc", {})),` after the `iam=` line (line 44 area). The `get(..., {})` default ensures backward compat for any code that loads config without a vpc section.

2. In `infra/config/dev.json`, add two changes:
   - Add `"trust_account_id": "339712815752"` inside the existing `"iam"` object (the Northshire Trust dev account ID).
   - Add a new top-level `"vpc"` object:
     ```json
     "vpc": {
       "platform_cidr": "10.10.0.0/16",
       "trust_cidr": "10.0.0.0/16",
       "max_azs": 2,
       "nat_gateways": 1
     }
     ```
   Place `"vpc"` after `"iam"` and before the closing brace. CIDRs are per D1 (PROJECT.md). `nat_gateways: 1` is per REQ-NET-01 (single-AZ NAT acceptable in dev).

3. In `infra/config/prod.json`, same changes:
   - Add `"trust_account_id": "339712815752"` inside `"iam"` (same Trust account for this portfolio project).
   - Add the identical `"vpc"` object. Prod uses same single NAT for this portfolio project per REQ-NET-01.

NOTE on trust_account_id: The value `339712815752` is the Northshire Trust simulator AWS account. If the user has a different account ID, they will update it — the plan must use a real-looking value, not `<placeholder>`.
  </action>
  <verify>
    <automated>cd /Users/chiamakaanamekwe/Documents/tech-projects/data-engineering/access-iq-platform && python -c "from access_iq_infra.settings import load_env_config; c = load_env_config('dev'); assert c.vpc['platform_cidr'] == '10.10.0.0/16'; assert c.iam['trust_account_id'] == '339712815752'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `EnvConfig` dataclass has field `vpc: dict[str, Any]` between `iam` and `tags`
    - `load_env_config` includes `vpc=dict(raw.get("vpc", {})),`
    - `infra/config/dev.json` has `"vpc": {"platform_cidr": "10.10.0.0/16", "trust_cidr": "10.0.0.0/16", "max_azs": 2, "nat_gateways": 1}`
    - `infra/config/dev.json` `"iam"` object contains `"trust_account_id": "339712815752"`
    - `infra/config/prod.json` has identical `vpc` and `trust_account_id` additions
    - `grep -c "vpc" infra/access_iq_infra/settings.py` returns 2 (field + loader)
  </acceptance_criteria>
  <done>EnvConfig loads vpc config from both dev and prod JSON files without error; existing fields unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Update all existing test _cfg() helpers with vpc={}</name>
  <files>
    tests/unit/test_lake_stack.py,
    tests/unit/test_ecr_stack.py,
    tests/unit/test_secrets_stack.py,
    tests/unit/test_catalog_stack.py,
    tests/unit/test_app_synth.py
  </files>
  <read_first>
    tests/unit/test_lake_stack.py,
    tests/unit/test_ecr_stack.py,
    tests/unit/test_secrets_stack.py,
    tests/unit/test_catalog_stack.py,
    tests/unit/test_app_synth.py
  </read_first>
  <action>
In each of the 5 test files, find the `_cfg()` helper function and add `vpc={},` to the `EnvConfig(...)` constructor call, placed after the `iam=` line and before `tags=`. The value is `{}` (empty dict) because these existing stacks do not consume vpc config.

Exact files and the line to add `vpc={},` after:
- `tests/unit/test_lake_stack.py` — after `iam={"external_bucket": "northshire-trust-external-exports"},`
- `tests/unit/test_ecr_stack.py` — after the `iam=` line
- `tests/unit/test_secrets_stack.py` — after the `iam=` line
- `tests/unit/test_catalog_stack.py` — after the `iam=` line
- `tests/unit/test_app_synth.py` — after the `iam=` line

Do NOT change any other lines in these files. This is a mechanical addition of one keyword argument to maintain compatibility with the frozen dataclass change from Task 1.
  </action>
  <verify>
    <automated>cd /Users/chiamakaanamekwe/Documents/tech-projects/data-engineering/access-iq-platform && make test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "vpc=" tests/unit/test_lake_stack.py` returns at least 1
    - `grep -c "vpc=" tests/unit/test_ecr_stack.py` returns at least 1
    - `grep -c "vpc=" tests/unit/test_secrets_stack.py` returns at least 1
    - `grep -c "vpc=" tests/unit/test_catalog_stack.py` returns at least 1
    - `grep -c "vpc=" tests/unit/test_app_synth.py` returns at least 1
    - `make test` exits 0 with all existing tests passing
    - `make ci` exits 0 (fmt + lint + type + test all green)
  </acceptance_criteria>
  <done>All 5 existing test files include `vpc={}` in their `_cfg()` helpers. `make ci` passes with zero failures.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Config file to EnvConfig | Config JSON parsed into frozen dataclass; untrusted if user edits JSON manually |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | infra/config/{dev,prod}.json trust_account_id | accept | AWS account IDs are not secrets (AWS docs confirm); needed for cross-account peering ARN construction |
| T-02-02 | Tampering | settings.py load_env_config | mitigate | Existing `raw.get("vpc", {})` with `dict()` wrapping prevents injection; frozen dataclass prevents mutation after construction |
</threat_model>

<verification>
1. `python -c "from access_iq_infra.settings import load_env_config; c = load_env_config('dev'); print(c.vpc)"` prints the vpc dict
2. `python -c "from access_iq_infra.settings import load_env_config; c = load_env_config('prod'); print(c.vpc)"` prints the vpc dict
3. `make test` — all existing tests pass
4. `make ci` — full CI gate green
</verification>

<success_criteria>
- EnvConfig.vpc field exists and loads from config JSON
- Both config files contain D1 CIDRs and trust_account_id
- All 5 existing test _cfg() helpers updated with vpc={}
- `make ci` passes
</success_criteria>

<output>
After completion, create `.planning/phases/02-networking/02-01-SUMMARY.md`
</output>
